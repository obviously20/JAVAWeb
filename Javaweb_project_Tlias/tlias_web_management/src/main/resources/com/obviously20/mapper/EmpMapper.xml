<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.obviously20.mapper.EmpMapper">
<!--    更新员工基本信息 -->
<!--    <set>标签：动态SQL语句的set标签，用来拼接update语句的set关键字，会自动去除set关键字后面多余的逗号。-->
    <update id="updateById">
        update emp
        <set>
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="job != null">job = #{job},</if>
            <if test="salary != null">salary = #{salary},</if>
            <if test="image != null and image != ''">image = #{image},</if>
            <if test="entryDate != null">entry_date = #{entryDate},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>
    <!--    <select id="list" resultType="com.obviously20.pojo.Emp">-->
<!--        select emp.*,dept.name as deptName from emp left join dept on emp.dept_id = dept.id-->
<!--        where emp.name like concat('%',#{name},'%') and-->
<!--        emp.gender = #{gender} and-->
<!--        emp.entry_date between #{begin} and #{end}-->
<!--        order by emp.update_time desc-->
<!--    </select>-->

<!--    todo 优化2：动态SQl:上面的语句写死了：必须都输入，才能查询到结果，不是必须都输入，而是根据用户输入的查询参数，动态拼接SQL语句-->
<!--        <where></where>:动态SQL语句的where标签，根据查询条件，来生成where关键字，并会自动去除条件前面多余的and或or。-->
<!--        <if></if>:判断条件是否成立，如果条件为true，则拼接SQL。-->
    <select id="list" resultType="com.obviously20.pojo.Emp">
        select emp.*,dept.name as deptName from emp left join dept on emp.dept_id = dept.id
        <where>
            <if test="name != null and name != ''">
                and emp.name like concat('%',#{name},'%')
            </if>
            <if test="gender != null">
                and emp.gender = #{gender}
            </if>
            <if test="begin != null and end != null">
                and emp.entry_date between #{begin} and #{end}
            </if>
        </where>
        order by emp.update_time desc
    </select>


    <!-- 定义empResultMap映射  手动封装 -->
<!--   todo Mybatis中封装查询结果，什么时候用 resultType，什么时候用resultMap ？-->
<!--        - 如果查询返回的字段名与实体的属性名可以直接对应上，用resultType 。-->
<!--        - 如果查询返回的字段名与实体的属性名对应不上，或实体属性比较复杂，可以通过resultMap手动封装 。-->

    <resultMap id="empResultMap" type="com.obviously20.pojo.Emp">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="name" property="name" />
        <result column="gender" property="gender" />
        <result column="phone" property="phone" />
        <result column="job" property="job" />
        <result column="salary" property="salary" />
        <result column="image" property="image" />
        <result column="entry_date" property="entryDate" />
        <result column="dept_id" property="deptId" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />

<!--        封装exprList-->
        <collection property="exprList" ofType="com.obviously20.pojo.EmpExpr" >
            <result column="eeId" property="id" />
            <result column="eeEmpId" property="empId" />
            <result column="eeBegin" property="begin" />
            <result column="eeEnd" property="end" />
            <result column="eeCompany" property="company" />
            <result column="eeJob" property="job" />
        </collection>
    </resultMap>


      <!-- 根据id查询员工详情 -->
    <select id="selectById" resultMap="empResultMap">
        select e.*,
        ee.id as eeId,
        ee.emp_id as eeEmpId,
        ee.begin as eeBegin,
        ee.end as eeEnd,
        ee.company as eeCompany,
        ee.job as eeJob
        from emp e left join emp_expr ee on e.id = ee.emp_id where e.id = #{id}
    </select>

    <!--职位人数统计 -->
    <select id="countPosNum" resultType="java.util.Map">
        select (case when job=1 then '班主任'
        when job=2 then '讲师'
        when job=3 then '学工主管'
        when job=4 then '教研主管'
        when job=5 then '咨询师'
        else '其他'
        end) pos
        ,count(*) num from emp
        group by job
        order by num;
    </select>

<!--    员工性别人数统计 -->
    <select id="countGenderNum" resultType="java.util.Map">
        select if(gender=1,'男性员工','女性员工') name
        ,count(*) value
        from emp
        group by gender
    </select>

    <!-- 批量删除员工（根据id列表） -->
    <delete id="deleteByIds" parameterType="java.util.List">
        delete from emp where id in
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
                #{id}
        </foreach>
    </delete>

</mapper>